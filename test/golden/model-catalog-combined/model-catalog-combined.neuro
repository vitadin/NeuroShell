%% Combined options test
%% Tests using multiple options together
\model-catalog[provider=openai, sort=name]
\model-catalog[provider=anthropic, search=claude, sort=name]
\model-catalog[search=gpt, sort=provider]