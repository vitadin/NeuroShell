%% Test check command quiet mode
\set[_echo_commands="true"]
%% This test verifies that \check[quiet=true] doesn't produce console output
%% but still sets the result variables
\check[quiet=true]
\echo Status: ${_check_status}
\echo Total services: ${_check_total_services}
\echo Failed count: ${_check_failed_count}