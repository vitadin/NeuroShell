%% Test that \run correctly pushes scripts to stack service
%% This verifies the stack functionality rather than immediate execution

%% Run a script
\run test/fixtures/simple-script.neuro

%% Check if the stack service has commands (indirect verification)
%% We can't directly access stack service in scripts, but we can verify behavior