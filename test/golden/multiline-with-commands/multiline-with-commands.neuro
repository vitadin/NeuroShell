%% Commands with multiline continuation test
%% Tests multiline functionality with various neuro commands

%% Test with \set command
\set[message="Hello World"]
\get[message]

%% Test with \echo command  
\echo This is an important message ...
that needs to be on multiple lines ...
for better readability

%% Test with variable interpolation in multiline
\set[greeting="Hello"]
\set[name="World"]
\echo ${greeting} ...
${name} from multiline